# soroban-vault

Next.js + TypeScript starter scaffold.

Requirements:
- Node >= 22
- npm

Setup:
1. npm install
2. npm run dev
3. Open http://localhost:3000

Notes:
- To run type checks: npm run type-check
